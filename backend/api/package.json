{
  "name": "@restaurant/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json",
    "db:migrate": "sh -ac 'set -a; [ -f ../.env ] && . ../.env; [ -f ./.env ] && . ./.env; [ -n \"$DATABASE_URL\" ] || { echo \"DATABASE_URL is not set\"; exit 1; }; psql -v ON_ERROR_STOP=1 -d \"$DATABASE_URL\" -f sql/migrate.sql'",
    "db:schema": "pnpm db:migrate",
    "db:seed": "sh -ac 'set -a; [ -f ../.env ] && . ../.env; [ -f ./.env ] && . ./.env; [ -n \"$DATABASE_URL\" ] || { echo \"DATABASE_URL is not set\"; exit 1; }; psql -v ON_ERROR_STOP=1 -d \"$DATABASE_URL\" -f sql/seed.sql'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.23",
    "@types/node": "^20.19.18",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2"
  }
}
